[build]
builder = "NIXPACKS"
buildCommand = "cd backend && npm install && npm run build && npm run migration:run"

[deploy]
startCommand = "cd backend && npm run start:prod"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

